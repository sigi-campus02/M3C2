"""Parameter serialization utilities.

This module centralizes helpers for persisting and restoring the
parameters used throughout the pipeline.  Parameter dictionaries can be
serialized to standard formats like JSON for storage, logging or
reproducible execution, and deserialized back into Python objects when
needed.
"""
