# rms = float(np.sqrt(np.mean(values ** 2)))
# outlier_mask = np.abs(clipped) > (outlier_rmse_multiplicator * rms)

# values -> mov (xyz coordinates) + distances (nur distanzwerte) 
# auf distances wird rms und outlier berechnet
# daraus wieder .ply erzeugen

